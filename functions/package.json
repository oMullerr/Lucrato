{
  "name": "lucrato-functions",
  "description": "Cloud Functions do Lucrato — integração com a API do Mercado Livre (somente leitura).",
  "private": true,
  "type": "commonjs",
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "test": "jest src",
    "deploy": "npm run build && firebase deploy --only functions",
    "test:rules": "jest test"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.11",
    "typescript": "~5.5.2"
  }
}
